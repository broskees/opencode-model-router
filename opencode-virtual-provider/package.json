{
  "name": "opencode-virtual-provider",
  "version": "0.1.0",
  "description": "OpenCode plugin that adds virtual models — configurable aliases that route to real provider models with fallback, retry, and rotation strategies",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "typecheck": "bun x tsc --noEmit",
    "test": "bun test",
    "dev": "bun run src/index.ts"
  },
  "keywords": [
    "opencode",
    "opencode-plugin",
    "ai",
    "model-router",
    "llm",
    "fallback",
    "virtual-model"
  ],
  "author": "",
  "license": "MIT",
  "peerDependencies": {
    "@opencode-ai/plugin": "^1.2.6"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.2.6",
    "opencode-anthropic-auth": "^0.0.13",
    "opencode-openai-codex-auth": "^4.4.0"
  },
  "devDependencies": {
    "bun-types": "^1.3.9",
    "typescript": "^5.7.3"
  }
}
